<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MinhaMega.Views.HomePage"
             xmlns:viewmodels="clr-namespace:MinhaMega.ViewModels"
             x:DataType="viewmodels:HomePageViewModel"
             Title="HomePage">

    <Grid  Margin="1">
        <Label FontSize="15" BackgroundColor="Aqua" Text="ssss" VerticalOptions="Center" HorizontalOptions="Center" />
        <Label Text="{Binding Mega.Acumulado}" VerticalOptions="Center" HorizontalOptions="Center" />
        <Label Text="{Binding Mega.DataProximoConcurso}" VerticalOptions="Center" HorizontalOptions="Center" />
        <Label Text="{Binding Mega.ListaDezenas}" VerticalOptions="Center" HorizontalOptions="Center" />
        <Label Text="{Binding Mega.ValorEstimadoProximoConcurso}" VerticalOptions="Center" HorizontalOptions="Center" />
        <Label Text="{Binding Mega.ListaRateioPremio}" VerticalOptions="Center" HorizontalOptions="Center" />
        <Label Text="{Binding Mega.ListaMunicipioUFGanhadores}" VerticalOptions="Center" HorizontalOptions="Center" />
        <Label></Label>
        <Label></Label>
        <Label></Label>
        <Label></Label>
        <Label></Label>
    </Grid>
</ContentPage>